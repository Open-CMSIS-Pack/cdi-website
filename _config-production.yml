# In order to build for staging run:
# $ jekyll build --config _config.yml, _config-production.yml
#
#
# URL of static site
url: "https://www.open-cmsis-cdi.org"
# Destination folder
destination: www.open-cmsis-cdi.org
# Production boolean used in layouts / includes
production: true
# Google Analytics
# google_analytics:
#   enabled: true
#   code: G-HMVFZEVJSK
